I've loaded the linter-building skill and references. Rule classification: this needs **type facts** (is the last result `error`? which package does the callee come from?) → `go/analysis` with types, per the skill's decision tree. The fixture in main.go:6 is a live trigger, good for verification.Deps resolved (x/tools v0.50.0, go bumped to 1.26.0). Now the analyzer:Now removing leftover cruft I accidentally included (dead `sortFindings` hack) and fixing the missing `used` field:Diagnostics shown were pre-edit (stale). Now the test corpus, tests, and CLI:testdata `unused` warnings are expected (fixtures are never referenced). The stale `d.used` typecheck needs real verification — building and testing now:gofmt rewrote it — re-reading the section first:Two known bugs (comment-node leak in own-line detection; `// want` merging into directive text) plus two unexplained misses at positive.go:15/19 — probing empirically:Harness fires for both — analyzer logic is correct, something's off with analysistest interaction. Isolating with a minimal second package:Package b passes — the failure is multi-file interaction. Bisecting:Root causes found: (1) suppression matched by line across files, (2) `// want` comment merges into the directive comment and parses as its reason, (3) `*ast.Comment` nodes leak into the own-line check. Rewriting the analyzer with per-file scoping:Cleaning up probe artifacts (per policy, `trash` not `rm`) and re-running the suite:`own-line above a FuncDecl` needs whole-function scope (finding sits 2+ lines below the directive), and the mutant assertion is wrong (inverted rule *should* still fire on the described error — the point is it must not agree at the empty site). Fixing both:Now fixing the mutant assertion in the test:All green. Self-lint (dogfooding — main.go:6 should be the one real finding) plus a race run:Self-lint: exactly one finding at `main.go:6:9` with exit 3 (x/tools diagnostics convention); race + gofmt clean. Documenting:Final verification pass (the IDE diagnostics shown are stale — the toolchain is authoritative):Done. Rule E001 (`emptyerr`) — a `go/analysis` analyzer, since "is the last result `error`" and "which package is the callee from" are type facts; callees are matched by package path via `TypesInfo.Uses`, so aliases and dot-imports can't fool it.

**Layout**
- `emptyerr/emptyerr.go` — analyzer: flags `errors.New("")` / `fmt.Errorf("")` with a constant empty first argument inside functions whose last result is `error`; reports at the construction site with an actionable message
- `cmd/emptyerr/main.go` — CLI (`go run ./cmd/emptyerr ./...`, exit 0 clean / 3 findings)
- `emptyerr/testdata/src/a/` — golden corpus (positives, negative filters, suppressions)
- `README.md` / `AGENTS.md` — usage, rule table, decisions

**Trust engineering** (per the linter-building skill): multi-signal detection with negative-filter fixtures (non-constant args, `" "`, non-error returns, package-level sentinels, shadowed constructors); reason-required `//emptyerr:ignore E001 <reason>` suppressions with staleness + missing-reason meta-findings, scoped per file (trailing line / statement above / whole function); truth-table tests plus a mutant test proving the corpus discriminates an inverted rule.

**Verified**: build, vet, `-race` tests, gofmt all green; self-lint finds exactly one real issue — your `main.go:6` `return errors.New("")` (left untouched since it's your demo code).

**Known limits** (documented): stdlib constructors only, result types must be identical to the `error` interface, no nested-closure scoping. If you later want SARIF/golangci-plugin output, the documented upgrade path is go-finding's `analysis.FromDiagnostic` bridge — no converter glue.
