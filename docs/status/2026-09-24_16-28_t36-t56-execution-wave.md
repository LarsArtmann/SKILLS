# Status Report — T36–T56 Execution Wave: Honesty Fix, Gate Hardening, Self-Tests, Eval Harness

**Date:** 2026-09-24 16:28 (Thursday, CEST)
**Session scope:** User instruction: READ/UNDERSTAND/RESEARCH/REFLECT, break the open
TODO list into actionable steps, execute and verify one at a time, repeat until done.
Scope = `TODO_LIST.md` open items T36–T56 (T30/T33/T34/T51 are BLOCKED and untouched;
T33's site work belongs to the site repos, not this one).
**Format:** markdown per the 9th-recurrence standing override (routed to ROADMAP).

---

## a) FULLY DONE

1. **SESSION-START executed** — feedback/new empty; newest report (09-18 15-05) TL;DR
   read; TODO_LIST read; AGENTS §8/§9 in context; `check-skills.sh` exit **0** quoted
   (30 skills, 150 md files) before any edit.
2. **T36 (honesty bug):** the "every comment across all repos" overclaim replaced with
   the `summary.json` collection parameters in `github-voice/SKILL.md`,
   `references/voice-profile.md`, the `collect-corpus.py` docstring, AND the
   `AGENTS.md` §5.5 paragraph (the TODO named one site; the same lie lived in four).
3. **T42+T43+T47 (gate + fixtures):** check 15 is fence- and quote-aware
   (`filler_scan` blanks fenced blocks and `>` quotes, line numbers preserved); the
   `It.s` regex replaced with explicit `It is|It's|It’s`; FAIL prints the remediation
   line. `--selftest` added to `check-skills.sh` running fixture mini-repos under
   `scripts/fixtures/check-skills/` (pass tree: fenced-filler + signal-fixture with
   pinned `preamble=6 prose=1 filler=0 jargon=1`; fail tree: throat-clearer yielding
   exactly one check-15 FAIL + remediation). `check-skill-links.sh` grew `--root`.
4. **T41:** `link-skills-to-agents.sh --selftest` (8-case sandbox matrix: fresh
   repair, wrong target, repair-heals, dangling, missing, orphan, real-dir conflict,
   repair-without-force-refuses) — 8/8 green; `--check` wired into `check-skills.sh`
   (skipped under `--root` and when `~/.agents/skills` is absent; gated otherwise;
   live run green).
5. **T37:** canonical `## Verification status` table in `buildflow/SKILL.md`
   (binary-verified vs ⚠️ date-stamped README claims; canon guard no longer warns).
6. **T38:** `naming-review` description 1008→848 chars (13 trigger markers, STRONG)
   with data-model-review disambiguation; data-model-review description 793→940
   names naming-review back; Related Skills cross-entry added. Trigger report: both
   STRONG.
7. **T39+T40:** check 14 hardening (row-level first-column matching + reverse
   deleted-skill direction — both firing proven on scratch trees); README↔FEATURES
   status-parity gate added and **it caught a live drift on first real run**:
   collector-extraction was 🟢 Solid in README but 🆕 NEW in FEATURES since 2026-09-10
   — FEATURES row corrected with the ssh-key-monitor v0.1.0 PASS evidence
   (CHANGELOG 2026-09-10 + SKILL.md battle-test log).
8. **T48 (round-3 f29, finally actually run):** `linter-building/evals/iteration-2/`
   via the REAL activation mechanism: eval-4 positive ("Write a linter…") — output
   opens "I've loaded the linter-building skill and references… per the skill's
   decision tree", PASS; eval-5 negative ("Lint my project.") — ran go
   build/vet/golangci-lint/art-dupl and offered the code-quality-scan dashboard, zero
   authoring content, PASS. `grading.json` with evidence quotes; limitation note:
   N=1 per prompt, single model.
9. **T49:** all ten research-sourced specifics in `linter-building/references/*`
   verified against local sources (ledger 90-day retention
   `pkg/audit/ledger.go:323`; loop cap `pipeline/config.go:95`; CompletionReasons
   `result.go:14-20`; H001 clusters `rule_bytes.go:20-25`; normLit
   `pattern_helpers.go:69`; ADR-0001 `docs/adr/0001-import-alias-detection.md`;
   `ContinueOnError()` `errors.go:62`; `RunWithSuggestedFixes` x/tools
   analysistest.go:82; tag scheme via `git tag -l`; `RuleMeta.Validate()`
   `rule.go:152`; black-box `package humanizelint_test`). Per-reference
   verification tables appended. **One inaccuracy found and fixed:** H001 fires on
   3+ unit strings ALONE (no division needed) — the reference's "requires strings
   AND division" was wrong.
10. **T44:** `originals/communication-doctrine.md` preserved with an honest
    provenance note (the verbatim paste was never persisted; reconstructed from the
    two processing reports) + AGENTS §5.1 note.
11. **T45:** `scripts/site-dod-check.sh <site> [--repo DIR]` — demo anchor, og:image
    with real 1200x630 decoding (pure-python PNG/JPEG/WebP header parser), mp4+JS
    immutable checks, firebase.json catch-all-order lint. Verified end-to-end
    against local fixture servers: good variant PASS (exit 0), bad variant fails all
    4 live checks, catch-all-first FAILs, catch-all-last PASSes.
12. **T46:** `scripts/run-eval.sh <eval-dir> [--cwd DIR]` — fresh non-interactive
    `crush run --quiet` session, fixture copy, timestamped `runs/<ts>/` with
    output.md/stderr.log/meta.json (model, exit, duration). Used for T48's two runs
    (92s and 1250s wall time). One fix during bring-up: `--yolo` is not a `run`
    flag.
13. **T50:** marker-vocabulary guard section-scoped (ANNOTATE must define `done at`/
    `Won't implement`/`NOT-DO`; HARVEST must reference the first two in its own
    section) — both firing directions proven on a scratch tree.
14. **T52 (restraint-disciplined):** github-voice preamble audited and compressed
    (corpus numbers deduplicated to profile §1; 31→27 preamble lines); go-release's
    "The definitive guide…" thesis removed; **all remaining flagged prose in
    website-launch (9 blocks), buildflow (3), docs-health (4), verify-before-filing
    audited and judged load-bearing (Pattern 10 keep) — no force-cuts.**
15. **T53/T54:** outcome-promise row added to the description quick-test table;
    grammar slip "file no later session reads" fixed in **3** live files (the plan
    said 2; grep found status-report/SKILL.md, harvest-guide.md,
    how-to-write-skills.md).
16. **T55:** SC2319 ×8 converted to `rc=0; [[ cond ]] || rc=1` in
    `validate-workflow.sh` (the old form ALSO aborted under `set -e` before check()
    could report — a real latent bug, not just style); SC2089/90 fixed via arrays in
    `naming-smells.sh` (+SC2064 fix-on-sight). **Proven with real runs:**
    shellcheck clean via `nix run nixpkgs#shellcheck`; validate-workflow 11/11 PASS
    exit 0; naming-smells real run exit 0.
17. **T56:** `annotate-markers.py` shared module (marker_for deduped; prose's
    `done (x)` unified to the corpus-majority `done — x`, 52 vs 35 files);
    `annotate-prose_test.py` written (h/v/p/w + UTC-default + end-to-end strike/guard
    behavior on a scratch file) — both test files pass, ruff clean, EXE001 fixed on
    check-rows.py.
18. **Bookkeeping:** TODO_LIST reduced to T30/T33/T34/T51 (the four BLOCKED);
    CHANGELOG wave entry written; README prose drift fixed on sight ("Seven 🆕" →
    Eight — buildflow was missing) + selftest mention; collector-extraction
    FEATURES row corrected (see a7).

## b) PARTIALLY DONE

1. **T52's --signal long tail is audited, not all rewritten.** The flagged prose
   blocks outside github-voice/go-release were each read and deliberately kept —
   they are decision-flipping why-paragraphs, exactly what Principle 7 says to
   keep. If a future pass wants lower prose counts, that is a judgment rewrite, not
   a mechanical one.

## c) NOT STARTED

1. T30/T33/T34/T51 — BLOCKED (real-world run / site-repo work / user decisions /
   glossary-lockstep decision). Untouched by design.

## d) TOTALLY FUCKED UP

1. **Introduced a silent-death bug into check-skills.sh the same session that built
   the selftest against silent deaths** — the parity gate's `grep … README.md |
   head -1` pipeline killed the script under `set -euo pipefail` when README.md was
   absent (every fixture mini-repo). The selftest caught it within one edit cycle
   (selftest 1/3 FAIL with no output past the inventory). Fixed with `|| true` on
   both capture pipelines. The irony is the lesson: the fixture harness earns its
   keep the first time it runs.
2. **First site-dod-check verification round had a broken harness** (two fixture
   servers sharing one docroot; the second overwrote the first's index.html) —
   script was right, test was wrong; isolated docroots fixed it. Same class as the
   05-44 d1 (reporting prose outran evidence) — caught before any claim was written.
3. Two multiedit old_strings failed on tab/whitespace mismatch (the arg-parsing
   block); retried with exact text per the edit rules. No damage.

## e) WHAT WE SHOULD IMPROVE

1. **Every new gate needs its negative test in the same commit** — T39/T40/T50 got
   scratch-tree proofs; a standing fail-fixture per check inside
   `scripts/fixtures/check-skills/` would make that automatic instead of
   per-session discipline.
2. **run-eval.sh should support seeded-session evals** (old-skill vs new-skill
   comparisons) — currently each run is a fresh session; the rewrite-eval workflow
   still needs a manual baseline export. Small: add `--session <id>` passthrough.
3. The parity gate compares marker CLASSES only; the README prose counts ("Seven
   skills…") drifted for two weeks undetected — prose counts are the next drift
   surface, but hard-gating prose numbers fights the no-hardcoded-counts rule. A
   WARN when a README number-word (seven/eight/…) disagrees with the row classes
   would be cheap.

## f) Next

Remaining open: T30, T33, T34, T51 — all blocked on the real world or user
decisions. Highest-value non-blocked follow-up: e1 (fail-fixtures per check) or
e2 (seeded-session evals) next session.

## g) Questions

None new; ROADMAP's standing open questions (glossary lockstep, prompt voice,
formatter scope) are untouched by this wave.

---

_Point-in-time snapshot. When stale, `docs-health` ANNOTATE — never rewrite._
