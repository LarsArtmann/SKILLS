# Prior art — jj agent skills survey

> Lookup material for skill authors. The operational rules that came out of
> this survey (the `.jj` safety rule and the `gh stack link` bridge) live in
> the skill body's "Prior art" section — this file only holds the survey
> itself, so future sessions do not redo it.

Checked 2026-09-08 via Sourcegraph + web search; re-checked 2026-09-10.

## Findings

- **Carbon Language's** `.agents/skills/jj/SKILL.md` — jj basics for Carbon
  contributors. No fork + multi-PR sync coverage; complementary, not a rival.
- A small `jj-commit` prompt snippet — single-commit message guidance only.
- **No public skill covers fork setup + the multi-PR sync loop** — this skill
  fills that gap. (Claim scoped to the jj sync loop itself, re-checked
  2026-09-10.)

Re-verify the "no public skill" claim before citing it externally: it is a
point-in-time survey, not a maintained fact.
