<!-- README.md template, copy into the project root and fill in.
     This is the SALES PAGE for end-users. Keep it user-facing.
     No internal architecture, no AI context, no developer notes.
     See docs-health/SKILL.md. -->

# Project Name

> One-line hook: what this does, in plain English. No jargon.

## Why

One paragraph: the problem this solves and why it exists.

## Installation

```bash
# exact commands that work on a clean machine
```

Prerequisites (if any):

- List anything that must be installed first

## Quick start

The simplest path from install to working result:

```bash
# step 1
# step 2
# step 3
```

## Usage

### Example 1: <most common use case>

```bash
# command or code
```

### Example 2: <second most common use case>

```bash
# command or code
```

---

<!-- Guidance for the builder:
  - Audience is end-users who do NOT know the project.
  - Test install commands mentally on a clean machine.
  - Feature claims must match FEATURES.md (if it exists).
  - Links must point at files that exist.
  - Keep it short: if a section exceeds 10 lines, link to a doc instead.
-->
