# Fixture FEATURES (fail tree)

Inventory row so the FEATURES coverage guard is not the failure this tree
asserts on. This file is fixture plumbing, not the real inventory.

| Skill          | Status | Notes                                     |
| -------------- | ------ | ----------------------------------------- |
| throat-clearer | DONE   | fixture: check 15 must hard-fail this one |
